name: Main

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  main:
    uses: ocadotechnology/codeforlife-workspace/.github/workflows/frontend.yaml@portal-frontend-48 # TODO: set to @main
    secrets: inherit
